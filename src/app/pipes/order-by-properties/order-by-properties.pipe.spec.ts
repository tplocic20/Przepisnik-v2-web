import { OrderByPropertiesPipe } from './order-by-properties.pipe';

describe('OrderByPropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByPropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
